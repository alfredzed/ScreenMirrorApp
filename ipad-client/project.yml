name: ScreenMirrorApp
options:
  deploymentTarget:
    iOS: "16.0"
settings:
  base:
    SWIFT_VERSION: "5.0"
targets:
  ScreenMirrorApp:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - path: Sources
    info:
      path: Info.plist
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.lise.screenmirror
        PRODUCT_NAME: ScreenMirrorApp
        TARGETED_DEVICE_FAMILY: "2"
        INFOPLIST_FILE: Info.plist
